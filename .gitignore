node_modules/
dist/
.env
.env.local
*.log
.DS_Store
Thumbs.db

# Bitacora de decisiones de uso local -- no se publica (puede contener
# contexto operativo interno). Sigue existiendo en disco, solo no se
# trackea en este repo.
MEMORY.md

# Investigacion interna y notas legales -- utiles localmente, pero
# mencionan contexto de portafolio/estrategia que no va a un repo
# publico. Igual que MEMORY.md: existen en disco, nunca se trackean.
investigacion/
legal/
