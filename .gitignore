node_modules/
dist/
.env
.env.local
*.log
.DS_Store
Thumbs.db

# Carpeta de estrategia (investigacion interna, legal, bitacora de
# decisiones, playbooks de uso local) -- es su propio repo git
# independiente (ver _strategy/README.md), nunca se trackea desde el
# repo publico de codigo. Existe en disco, no se publica aqui.
_strategy/

# Config/skills de Claude Code para este proyecto -- de uso local,
# nunca se trackea en el repo publico.
.claude/

# Skills de terceros instalados via `npx skills` (hedera-dev/hedera-skills)
# -- corren con permisos completos de agente, algunos con riesgo Med/High
# segun Snyk (ver skills.sh/hedera-dev/hedera-skills). Uso local, no se
# versionan en el repo.
.agents/
agent/
skills-lock.json
